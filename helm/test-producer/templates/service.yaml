apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-producer.fullname" . }}
  labels:
    {{- include "test-producer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-producer.selectorLabels" . | nindent 4 }}

